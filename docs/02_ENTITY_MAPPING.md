# 02. Матрица соответствий сущностей (Entity Mapping)

**Проект:** KeyCRM ↔ 1С:УТП для Украины · **Дата:** 2026-07-16 · **Этап:** 1

Обозначения направления:
- `1С → KeyCRM` — 1С владелец, данные текут в KeyCRM;
- `KeyCRM → 1С` — KeyCRM владелец, данные текут в 1С;
- `↔` — двунаправленно с правилом разрешения конфликтов.

Обозначения достоверности: ✅ факт · 🟡 типовое (подтвердить) · ❓ уточнить.

---

## Таблица 1. Соответствие сущностей и владение данными

| # | KeyCRM | 1С:УТП | Главная система | Направление | Уникальный ключ связи | Конфликты |
|---|---|---|---|---|---|---|
| 1 | Товар (`product`) | `Справочник.Номенклатура` | **1С** | 1С → KeyCRM | KeyCRM `sku` ↔ код/артикул 1С; хранить GUID 1С | 1С побеждает |
| 2 | Вариант (`offer`) | `Справочник.ХарактеристикиНоменклатуры` 🟡 | **1С** | 1С → KeyCRM | `offer.sku`/`offer.id` ↔ (Номенклатура+Характеристика) | 1С побеждает |
| 3 | SKU (`offer.sku`) | Код / Артикул 🟡 | **1С** | 1С → KeyCRM | строковое равенство SKU | 1С побеждает |
| 4 | Штрихкод (`barcode`) | `РС.ШтрихкодыНоменклатуры` 🟡 | **1С** | 1С → KeyCRM | barcode ↔ barcode | 1С побеждает |
| 5 | Категория (`category`) | Группа `Номенклатуры` / вид 🟡 | **1С** | 1С → KeyCRM | по коду/имени группы (таблица) | 1С побеждает |
| 6 | Цена (`offer.price`, `prices`) | `РС.ЦеныНоменклатуры` (`ТипЦен`) 🟡 | **1С** | 1С → KeyCRM | сопоставление вид цены ↔ прайс KeyCRM | 1С побеждает |
| 7 | Остаток (`/offers/stocks`) | `РН.ТоварыНаСкладах` 🟡 | **1С** | 1С → KeyCRM | `offer_id`+`warehouse_id` | **1С — единственный истинный остаток** |
| 8 | Склад (`warehouse_id`) | `Справочник.Склады` 🟡 | обе (справочник) | таблица соответствий | постоянная mapping-таблица (6 мест) | ручная сверка |
| 9 | Ед. изм. (`unit`/`measure`) | `Справочник.ЕдиницыИзмерения` 🟡 | **1С** | 1С → KeyCRM | таблица (шт↔шт, м↔м) | 1С побеждает |
| 10 | Покупатель (`buyer`) | `Справочник.Контрагенты` | **KeyCRM** | KeyCRM → 1С | `buyer.id` + поиск дублей по телефону/email | KeyCRM побеждает по карточке |
| 11 | Компания (`company`) | `Контрагенты`/партнёр 🟡 | **KeyCRM** | KeyCRM → 1С | `company.id` | KeyCRM |
| 12 | Контактное лицо | `КонтактныеЛицаКонтрагентов` 🟡 | **KeyCRM** | KeyCRM → 1С | телефон/email | KeyCRM |
| 13 | Заказ (`order`) | `Документ.ЗаказПокупателя` | **KeyCRM** | KeyCRM → 1С | `order.id` → GUID заказа 1С | KeyCRM (состав до отгрузки) |
| 14 | Позиция заказа | ТЧ «Товары» ЗаказаПокупателя | **KeyCRM** | KeyCRM → 1С | `offer.sku` + строка | KeyCRM |
| 15 | Резерв | резерв в ЗаказеПокупателя / `РН.ТоварыВРезерве` 🟡 | **1С** (факт) | KeyCRM→1С команда, 1С→KeyCRM факт | `order.id` | 1С подтверждает |
| 16 | Оплата (`payment`) | `ПКО`/`ПлатёжноеПоручениеВходящее` 🟡 | ↔ (см. правила) | двунаправленно | `payment.id` ↔ GUID платежа | журнал конфликтов |
| 17 | Реализация | `Документ.РеализацияТоваровУслуг` 🟡 | **1С** (факт) | KeyCRM→1С команда, 1С→KeyCRM номер | `order.id` → GUID реализации | 1С |
| 18 | Возврат (`return`) | `ВозвратТоваровОтПокупателя` 🟡 | **1С** (факт) | KeyCRM→1С команда, 1С→KeyCRM номер | `order.id`+состав возврата | 1С |
| 19 | Менеджер (`manager`) | `Справочник.Пользователи` 🟡 | обе (справочник) | таблица соответствий | mapping user↔user | ручная сверка |
| 20 | Статус заказа (`status`) | Состояние документов 1С | **KeyCRM** (процесс) | KeyCRM ведёт, 1С реагирует | таблица статус↔действие | KeyCRM |
| 21 | Доставка/ТТН | реквизиты доставки заказа 🟡 | **KeyCRM** | KeyCRM → 1С (справочно) | номер ТТН | KeyCRM |
| 22 | Валюта (`currency`) | `Справочник.Валюты` 🟡 | **1С** | фикс. **USD** (DEC-008) | код валюты | заказ ≠ USD → журнал конфликтов, ручной пересчёт (Prom) |

---

## Таблица 2. Ключи идентификации (анти-дубли, анти-циклы)

| Сущность | Первичный ключ связи | Резервный поиск | Где хранится обратная ссылка |
|---|---|---|---|
| Товар/вариант | KeyCRM `offer.sku` / `offer.id` | штрихкод | `РС.ВнешниеИдентификаторыKeyCRM` (в расширении 1С) |
| Склад | mapping-таблица (в middleware) | — | БД middleware `warehouse_map` |
| Заказ | KeyCRM `order.id` | — | кастомное поле заказа `1c_order_guid` + БД middleware |
| Покупатель | KeyCRM `buyer.id` | телефон + email | `РС.ВнешниеИдентификаторыKeyCRM` |
| Документ 1С | GUID ссылки 1С | — | кастомное поле заказа `1c_doc_number`/`1c_doc_guid` |
| Оплата | KeyCRM `payment.id` | — | БД middleware `payment_map` |
| Событие/вебхук | ключ идемпотентности (хэш) | — | БД middleware `idempotency_keys` |

**Правило анти-цикла:** запись, инициированная синхронизацией, помечается флагом
источника; входящее изменение с тем же флагом и неизменным содержимым **не порождает**
обратную синхронизацию (сравнение по контрольной сумме значимых полей).

---

## Таблица 3. Правила по каждой сущности

Для каждой строки Таблицы 1 фиксируются (заполняется по мере снятия блокеров):

- **Создание:** условие и обязательные поля (напр. Контрагент в 1С требует
  Договор+Организацию 🟡 — подтвердить).
- **Обновление:** какие поля обновляемы, какие «только чтение».
- **Удаление:** запрещено как способ учёта — вместо удаления архивация/сторно
  (ТЗ п.13, п.6 правил Claude).
- **Разрешение конфликтов:** по колонке «Конфликты» Таблицы 1; молчаливая
  перезапись запрещена (ТЗ п.12 оплаты) — при расхождении запись в журнал конфликтов.
- **Частота синхронизации:** товары/цены — по вебхуку + периодическая полная сверка;
  остатки — по вебхуку + периодическая сверка; заказы — по вебхуку статусов
  (near-real-time); полная сверка — по расписанию и по требованию из админ-панели.

> Детализация правил создания/проведения зависит от учётной логики УТП (B.5 в
> `01_SYSTEM_AUDIT.md`) и **заблокирована** до получения данных `04_DATA_REQUEST.md`.

---

## Таблица 4. Соответствие складов (6 мест) — ШАБЛОН

Заполняется реальными данными после снятия BLK-001/BLK-002.

| KeyCRM `warehouse_id` | KeyCRM название | 1С Склад (код) | 1С Склад (GUID) | 1С название | Направление | Активно | Комментарий |
|---|---|---|---|---|---|---|---|
| ❓ | Магазин №1 | ❓ | ❓ | ❓ | 1С→KeyCRM | ❓ | |
| ❓ | Магазин №2 | ❓ | ❓ | ❓ | 1С→KeyCRM | ❓ | |
| ❓ | Магазин №3 | ❓ | ❓ | ❓ | 1С→KeyCRM | ❓ | |
| ❓ | Магазин №4 (сборка) | ❓ | ❓ | ❓ | 1С→KeyCRM | ❓ | основная сборка |
| ❓ | Склад №1 | ❓ | ❓ | ❓ | 1С→KeyCRM | ❓ | |
| ❓ | Склад №2 | ❓ | ❓ | ❓ | 1С→KeyCRM | ❓ | |

(машиночитаемые шаблоны mapping — в `handover-1c/06_MAPPING_TABLES.csv`, будут созданы на Этапе 2.)
