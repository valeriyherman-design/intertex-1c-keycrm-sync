# PROJECT_STATUS — Интеграция KeyCRM ↔ 1С

**Дата обновления:** 2026-07-16
**Текущий этап:** Этап 3-5 — остатки + сквозной цикл заказа (резерв/реализация/отмена/оплата) реализованы и протестированы (56/56, E2E на живом KeyCRM); live за ГЕЙТом R-12
**Готовность основного цикла заказа:** middleware реализован и протестирован (56/56, E2E
на живом KeyCRM); в бою «на паузе» за ГЕЙТом R-12 (сопоставление штрихкодов) и доступом к 1С.

## Главная цель проекта

Весь ежедневный цикл работы менеджеров, удалённых сотрудников и сборщиков
максимально проходит через **KeyCRM**, без ручного дублирования заказов,
клиентов, товаров, счетов и данных в **1С**.

1С — внутреннее учётное ядро (склад + финансы). Сотрудники не заходят в 1С
для обычной обработки заказа.

## Профиль бизнеса (из устава, требует подтверждения)

| Параметр | Значение | Статус |
|---|---|---|
| Тип товара | Ткани/фурнитура: поштучно и в метрах (дробное кол-во) | ❓ подтвердить |
| Магазины | 4 | ❓ подтвердить |
| Склады | 2 | ❓ подтвердить |
| Основной интерфейс сотрудников | KeyCRM | ✅ из устава |
| Учётное ядро | 1С | ✅ из устава |

## Что сделано (Этапы 1–5)

**Этап 1 — обследование:** управляющие документы; `docs/01–06`, `KEYCRM_LIMITATIONS`
(LIM-01…04); живой снапшот KeyCRM; факты среды 1С (8.3.18, УТП 1.2.35.1, файловая);
анализ реальных выгрузок 1С (6 складов, единицы, сопоставление).

**Этап 2 — каркас middleware (.NET 8):** очередь SQLite WAL (дедуп, per-order
блокировка, backoff), клиент KeyCRM (60 rpm + retry), `I1CConnector` (COM-драйвер +
Mock), приём вебхуков, контракт API 1С + OpenAPI, исходник обработки `.epf`.

**Этап 3 — остатки:** `StockSyncService` (DEC-010: Σ 6 мест → KeyCRM), снапшот
last-pushed, diff-only, R-13 обнуление; укреплён по ревью (анти-обнуление, гонка,
клампинг). `NameNormalizer` (DEC-013). Каталог товаров — TODO (гейт R-12).

**Этап 4–5 — цикл заказа:** `KeyCrmOrderMapper` + `OrderProcessor` (резерв/реализация/
отмена/оплата, идемпотентность, USD-guard, ошибки → «Помилка синхронізації»);
`QueueWorker` маршрутизация по статусам. E2E на живом заказе 1780.

**Тесты: 56/56.** Всё live-действие за ГЕЙТом R-12 + dry-run по умолчанию.

## Что заблокировано (детали — `BLOCKERS.md`)

1. 🟡 BLK-001 KeyCRM: ключ получен, снапшот снят; остались данные кабинета
   (склады/ID, сотрудники/роли, события вебхуков, тариф).
2. 🟡 BLK-002 1С: платформа 8.3.18.1334, УТП 1.2.35.1, база файловая — факты
   получены; критично уточнить **режим совместимости** (Конфигуратор).
3. 🔴 BLK-003 тестовая копия базы 1С — не предоставлена.
4. 🔴 BLK-004 площадка middleware — вероятно, машина с базой (`D:\ALMITS\...`),
   подтвердить возможность установки .NET 8 сервиса.

## Ближайшие шаги

**Разблокирует всё (нужно от заказчика):**
- [ ] 🔴 ГЕЙТ R-12: выгрузка регистра Штрихкодов 1С (совпадают ли `2000000…`) —
      без этого сопоставление товаров недоказано, live-остатки/заказы «на паузе».
- [ ] Режим совместимости конфигурации (Конфигуратор) — расширение vs COM.
- [ ] Тестовая копия базы 1С + сборка `.epf` (BLK-003).

**Можно без доступов (продолжаю):**
- [ ] Каталог товаров (номенклатура → KeyCRM) на моке + dry-run.
- [ ] Пакет передачи 1С-специалисту (`handover-1c/`: 07/08/09/10, тест-кейсы, чек-лист).
- [ ] Админ-панель (состояние обмена, очередь, конфликты, ручной повтор).

## Приоритет реализации (не отклоняться до завершения основного цикла)

1. Товары и сопоставления → 2. Остатки → 3. Клиент → 4. Заказ → 5. Резерв →
6. Сборка → 7. Счёт/документы → 8. Реализация/списание → 9. Доставка/ТТН →
10. Отмены/возвраты → 11. Оплаты → 12. Перемещения → 13. Улучшения.
